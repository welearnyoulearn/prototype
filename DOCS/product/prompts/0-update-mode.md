# Prompt — update mode (after new features ship)

Attach the latest `docs/product/PRODUCT-FACTBOOK.md` **and** the previous deck/document.

---

The factbook was regenerated from `dev`. Compare it with the previous version of this deliverable.
1. List what is new, changed or removed (use the catalog table and the "Removed from dev" list).
2. Update ONLY those parts; keep everything else and the same style.
3. Never claim anything in section 7 "Honest limits". Use "[TBD – founder]" for missing facts.
4. End with a short change log.
