# Feature: Exam Schedule & Marks

**Platform Admin feature key:** `exam-marks`
**Portal:** School Admin / Teacher / Student / Parent
**Status:** Built (working on dev)
**Last verified against the code:** 2026-09-21

---

## What it does

Create exams, collect marks from teachers, review, release, and let parents acknowledge.

## Who uses it

School admin (create/review/release), teachers (enter marks), students and parents (results).

## How it works

- The admin **schedules** an exam across one or many classes; each class's subjects and subject teachers are copied from that class's own subject list.
- Status flow: `scheduled` → `collecting` (a nightly job opens it the day after the exam date) → `teacher_reviewed` (the **class teacher** reviews and forwards) → `released` (the **admin** releases; irreversible).
- Subject teachers enter and submit marks for their own subject; the class teacher may reopen a submitted subject **only before review**.
- Students and parents see results **only after release**; parents **acknowledge**, and the admin sees who has not and can nudge.

## Rules and limits

- One shared grading ladder (`lib/examGrading.ts`) so a score shows the same grade everywhere.
- Pass mark is set per exam.

## Code evidence

<!-- AUTO:evidence:exam-marks:start (generated by scripts/product-docs.mjs — do not edit by hand) -->
**Code evidence** — checked against the repository on every run of `node scripts/product-docs.mjs`.

Screens: `app/school-admin/components/ExamSchedule.tsx`, `app/teacher/components/ExamMarks.tsx`, `app/student/components/StudentMarks.tsx`

API routes these screens call (all exist):
- `GET/POST` /api/classes
- `DELETE/GET/PUT` /api/classes/{}
- `GET` /api/exams
- `POST` /api/exams/schedule
- `DELETE/GET/PUT` /api/exams/{}
- `GET` /api/exams/{}/acknowledgements
- `GET/POST` /api/exams/{}/marks
- `POST` /api/exams/{}/nudge-parent
- `POST` /api/exams/{}/release
- `POST` /api/exams/{}/review
- `POST` /api/exams/{}/subjects
- `POST` /api/exams/{}/subjects/{}/reopen
- `GET/POST` /api/students
- `GET` /api/students/{}/exams

✅ Every API route the screens call exists.
<!-- AUTO:evidence:exam-marks:end -->

## Related

- Deep dossier: [Exam Schedule & Marks](../../docs/product/features/10-exam-schedule-and-marks.md)
- [students.md](students.md)
- [export.md](export.md)

## Status history

| Date | Change | Issue |
|------|--------|-------|
| 2026-09-21 | Doc created from the code; status checked with `scripts/product-docs.mjs` | #162 |
