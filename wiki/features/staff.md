# Feature: Staff Directory & Onboarding

**Platform Admin feature key:** `staff`
**Portal:** School Admin
**Status:** Built (working on dev)
**Last verified against the code:** 2026-09-21

---

## What it does

Add and manage teachers and other staff, one by one or in bulk.

## Who uses it

School admin.

## How it works

- Add a teacher with a form, or upload a CSV/Excel (`/api/teachers/parse-import`, `/api/teachers/bulk`; a template is downloadable).
- The system creates an employee id and a temporary password; the teacher signs in with email + password and must change it.
- Edit, search, filter by department/staff type; removing a teacher is a soft delete (they stop being able to mark attendance).
- School-admin-level staff (principal, vice principal, more admins) are invited from **School Settings → Staff accounts** with a one-time set-password link — see settings.md.

## Rules and limits

- Plan limits (staff_limit) are enforced when accounts are created.
- Teachers are scoped to their school; a deactivated teacher cannot mark attendance.

## Code evidence

<!-- AUTO:evidence:staff:start (generated by scripts/product-docs.mjs — do not edit by hand) -->
**Code evidence** — checked against the repository on every run of `node scripts/product-docs.mjs`.

Screens: `app/school-admin/components/StaffOnboarding.tsx`, `app/school-admin/components/TeachersManagement.tsx`

API routes these screens call (all exist):
- `GET` /api/admin/overview
- `DELETE/GET/POST` /api/platform/subjects
- `GET` /api/school/subjects
- `GET` /api/teachers
- `POST` /api/teachers/bulk
- `POST` /api/teachers/parse-import
- `GET` /api/teachers/template
- `DELETE/GET/PUT` /api/teachers/{}

✅ Every API route the screens call exists.
<!-- AUTO:evidence:staff:end -->

## Related

- [settings.md](settings.md)
- [class-management.md](class-management.md)
- [auth.md](auth.md)

## Status history

| Date | Change | Issue |
|------|--------|-------|
| 2026-09-21 | Doc created from the code; status checked with `scripts/product-docs.mjs` | #162 |
