# Feature: Student Portal Access

**Platform Admin feature key:** `student-portal`
**Portal:** Student
**Status:** Built (working on dev)
**Last verified against the code:** 2026-09-24

---

## What it does

Lets a school give its students their own portal at `/student`.

## Who uses it

Students (own data only).

## How it works

- **Look and feel (#229):** a student-only "sticker-book" design — bold outlined cards, 3D Fluent Emoji stickers, a display + handwritten font pair, and a per-screen colour (the page wash and the active nav chip change colour per section). Each screen uses the pattern that fits its job: dashboard quest tiles and a notice pinboard, syllabus subject folders with notebook chapters, a library bookshelf, birthday greeting cards with chat-bubble wishes, a report-card marks page with a certificate score card, a habit-tracker attendance calendar, a wall-planner school calendar, an ID-card profile and retro-window AI Hub. The school's uploaded logo shows top-left, as in the school admin portal. Styles live in `app/student-ui.css`, scoped to `[data-student-ui]`, so no other portal changes.
- Dashboard (attendance note, quests, announcements), Syllabus progress, Digital Library, **My Attendance** (own calendar, month/year %, trend, upcoming holidays), School Calendar, My Marks, Class Circle (classmates' birthdays), profile and password.
- **AI Hub** is a page of links to external AI assistants (Gemini, Claude, ChatGPT), shown only for schools with AI access enabled — it is not a built-in tutor.
- Removed from `dev` (each preserved on its own branch): the Learning Hub and Daily Knowledge (#164), the weekly test (#168) and the rewards marketplace (#165).

## Rules and limits

- A student sees only themselves; class-level attendance is never exposed.
- Which tabs appear is controlled by the school's plan (feature switches).

## Code evidence

<!-- AUTO:evidence:student-portal:start (generated by scripts/product-docs.mjs — do not edit by hand) -->
**Code evidence** — checked against the repository on every run of `node scripts/product-docs.mjs`.

Screens: `app/student/page.tsx`, `app/student/components/stickers.tsx`, `app/student/components/StudentAiHub.tsx`, `app/student/components/StudentClassCircle.tsx`, `app/student/components/StudentDashboard.tsx`, `app/student/components/StudentExperience.tsx`, `app/student/components/StudentMarks.tsx`, `app/student/components/StudentProfile.tsx`, `app/student/components/StudentSyllabus.tsx`

API routes these screens call (all exist):
- `GET` /api/academic-year/current
- `GET/POST` /api/classes
- `GET` /api/school/enabled-features
- `GET` /api/school/library
- `GET` /api/school/subjects/materials
- `GET/PUT` /api/schools/{}/ai-access
- `GET` /api/student/attendance
- `POST` /api/student/auth/change-password
- `PUT` /api/student/auth/date-of-birth
- `POST` /api/student/auth/logout
- `GET` /api/student/auth/me
- `GET` /api/student/class-circle
- `POST` /api/student/class-circle/wish
- `GET` /api/students/{}/exams
- `DELETE/GET/POST` /api/syllabus

✅ Every API route the screens call exists.
<!-- AUTO:evidence:student-portal:end -->

## Related

- Deep dossier: [Student Portal](../../docs/product/features/05-student-portal.md)
- [attendance.md](attendance.md)
- [exam-marks.md](exam-marks.md)
- [curriculum.md](curriculum.md)

## Status history

| Date | Change | Issue |
|------|--------|-------|
| 2026-09-21 | Doc created from the code; status checked with `scripts/product-docs.mjs` | #162 |
| 2026-09-24 | Sticker-book redesign of every student screen; school logo in the sidebar | #229 |
