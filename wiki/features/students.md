# Feature: Students List & Onboarding

**Platform Admin feature key:** `students`
**Portal:** School Admin
**Status:** Built (working on dev)
**Last verified against the code:** 2026-09-21

---

## What it does

Add students, manage them, and open a complete profile of any student.

## Who uses it

School admin.

## How it works

- Add one student or bulk-upload a CSV/Excel template; duplicates are detected before saving and can be cleaned up.
- Each student gets a system id and a class roll number (unique within grade + section); the parent phone must be a valid 10-digit Indian mobile.
- Parent accounts are created and linked automatically; student and parent login credentials are issued and can be reset.
- Edit, promote, deactivate. Click a name to open the **Student 360 profile** (`GET /api/students/{id}/profile`): talking points for the parent meeting, contacts, attendance, marks, fees, activity, and a year switcher for past years.

## Rules and limits

- Every query is scoped to the admin's school; the profile is read-only.

## Code evidence

<!-- AUTO:evidence:students:start (generated by scripts/product-docs.mjs — do not edit by hand) -->
**Code evidence** — checked against the repository on every run of `node scripts/product-docs.mjs`.

Screens: `app/school-admin/components/StudentOnboarding.tsx`, `app/school-admin/components/StudentsManagement.tsx`, `app/school-admin/components/StudentProfile.tsx`

API routes these screens call (all exist):
- `GET` /api/admin/overview
- `GET/POST/PUT` /api/attendance
- `GET/POST` /api/school-admin/students/backfill-portal
- `GET/POST` /api/students
- `POST` /api/students/bulk
- `POST` /api/students/check-duplicates
- `GET` /api/students/duplicates
- `POST` /api/students/duplicates/cleanup
- `GET` /api/students/template
- `DELETE/GET/PUT` /api/students/{}
- `GET` /api/students/{}/profile
- `POST` /api/students/{}/reset-credentials

✅ Every API route the screens call exists.
<!-- AUTO:evidence:students:end -->

## Related

- Deep dossier: [Students](../../docs/product/features/03-students-list-and-onboarding.md)
- [class-management.md](class-management.md)
- [attendance.md](attendance.md)
- [fee-management.md](fee-management.md)
- [exam-marks.md](exam-marks.md)

## Status history

| Date | Change | Issue |
|------|--------|-------|
| 2026-09-21 | Doc created from the code; status checked with `scripts/product-docs.mjs` | #162 |
