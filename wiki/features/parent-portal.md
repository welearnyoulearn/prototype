# Feature: Parent Portal Access

**Platform Admin feature key:** `parent-portal`
**Portal:** Parent
**Status:** Built (working on dev)
**Last verified against the code:** 2026-09-21

---

## What it does

Lets a school give parents a read-mostly portal at `/parent` for their own children.

## Who uses it

Parents (own children only).

## How it works

- Child summary, **Attendance** (colour calendar, month/year %, days absent, full-days-in-a-row streak, trend, upcoming holidays), Fees (ledger, waivers, payment history, report a UPI payment), Exam calendar and Results (with acknowledgement), School Calendar, Syllabus, Digital Library.
- Several children per parent; English and Telugu interface.
- Sign-in by phone/email + password. Forgot-password by WhatsApp OTP is a **draft PR (#155)** and not part of dev.

## Rules and limits

- A parent can read only children linked to them; any other student id is answered as 'not found'.

## Code evidence

<!-- AUTO:evidence:parent-portal:start (generated by scripts/product-docs.mjs — do not edit by hand) -->
**Code evidence** — checked against the repository on every run of `node scripts/product-docs.mjs`.

Screens: `app/parent/page.tsx`, `app/parent/components/ParentProfile.tsx`, `app/parent/components/ParentSyllabus.tsx`

API routes these screens call (all exist):
- `GET` /api/academic-year/current
- `GET/PATCH/POST/PUT` /api/academic-years
- `GET/POST` /api/announcements
- `GET/POST` /api/classes
- `POST` /api/exams/{}/acknowledge
- `GET` /api/fees/upi-qr
- `GET` /api/fees/upi-qr/info
- `GET` /api/parent/attendance
- `POST` /api/parent/auth/change-password
- `PUT` /api/parent/auth/date-of-birth
- `POST` /api/parent/auth/logout
- `GET` /api/parent/auth/me
- `GET` /api/parent/child-summary
- `GET/POST` /api/parent/fees
- `GET` /api/school/enabled-features
- `GET` /api/school/library
- `DELETE/GET/POST` /api/syllabus

✅ Every API route the screens call exists.
<!-- AUTO:evidence:parent-portal:end -->

## Related

- [attendance.md](attendance.md)
- [fee-management.md](fee-management.md)
- [online-payments.md](online-payments.md)
- [exam-marks.md](exam-marks.md)
- [parent.md](parent.md)

## Status history

| Date | Change | Issue |
|------|--------|-------|
| 2026-09-21 | Doc created from the code; status checked with `scripts/product-docs.mjs` | #162 |
