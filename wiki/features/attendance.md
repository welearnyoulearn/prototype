# Feature: Attendance Tracking

**Platform Admin feature key:** `attendance`
**Portal:** School Admin / Teacher / Student / Parent
**Status:** Built (working on dev)
**Last verified against the code:** 2026-09-21

---

## What it does

Daily attendance for every class, with locking, holidays, dashboards for every role, and one set of rules so the numbers match everywhere.

## Who uses it

Any teacher marks; class teachers get a class dashboard; the school admin sees the whole school; parents and students see their own.

## How it works

- Any teacher can mark any class, Morning or Afternoon. The **first submit locks the session**; others see 'Already marked by …'.
- The marker can correct the same day, the admin any time, other teachers use 'Report a mistake'.
- Holidays and weekly-off days (Academic Calendar) cannot be marked and are excluded from every percentage.
- Dashboards: school admin *Overview* (school → class → student), class teacher *My class*, parent/student calendar.
- Exports: class register and daily absentee list (CSV); absence emails to parents.

- **Absentees view (Day register):** Attendance → Day register → **Absentees** shows everyone absent in Morning (or Afternoon) on one page, class by class — roll number, name, parent and a tap-to-call phone number, and a **Whole day** / **Morning only** tag. Classes that have not marked that session are listed at the top; classes with no absentees are summed up in one line. Print, or download the day's list as CSV. School admins only.

## Rules and limits

- Late counts as attended; % = attended ÷ marked sessions; bands 90% / 75%; a dashboard needs 4 marked sessions before labelling a student.
- Teachers may mark today and 2 days back; nobody may mark the future. 'Today' is India time.

## Code evidence

<!-- AUTO:evidence:attendance:start (generated by scripts/product-docs.mjs — do not edit by hand) -->
**Code evidence** — checked against the repository on every run of `node scripts/product-docs.mjs`.

Screens: `app/school-admin/components/AttendanceDashboard.tsx`, `app/school-admin/components/AttendanceOverviewDashboard.tsx`, `app/school-admin/components/AttendanceTodayPanel.tsx`, `app/teacher/components/Attendance.tsx`, `app/teacher/components/attendance/ClassPicker.tsx`, `app/teacher/components/attendance/HistoryView.tsx`, `app/teacher/components/attendance/MarkSheet.tsx`, `app/teacher/components/attendance/types.ts`, `app/components/AttendanceCalendar.tsx`, `app/components/attendance-dashboard/ClassDashboard.tsx`, `app/components/attendance-dashboard/parts.tsx`, `app/components/attendance-dashboard/StudentModal.tsx`

API routes these screens call (all exist):
- `GET/POST/PUT` /api/attendance
- `GET` /api/attendance/dashboard
- `GET` /api/attendance/overview
- `GET/PATCH/POST` /api/attendance/report
- `GET` /api/export/attendance
- `GET` /api/parent/attendance
- `GET` /api/student/attendance

✅ Every API route the screens call exists.
<!-- AUTO:evidence:attendance:end -->

## Related

- [../../docs/ATTENDANCE.md](../../docs/ATTENDANCE.md)
- [calendar.md](calendar.md)
- [students.md](students.md)
- [export.md](export.md)

## Status history

| Date | Change | Issue |
|------|--------|-------|
| 2026-09-21 | Doc created from the code; status checked with `scripts/product-docs.mjs` | #162 |
| 2026-09-22 | Day register: Absentees view (all absentees on one page, class-wise) | #208 |
